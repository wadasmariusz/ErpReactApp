import {BASE_URL} from "app/config/env";

const prefix = `${BASE_URL}`;

export const myAccountRoutes = {
  'app.myAccount': `${prefix}/my-account`,
  'app.myAccount.edit': `${prefix}/my-account/edit`,
}
